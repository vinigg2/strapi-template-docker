'use strict';

/**
 *  distribution-center controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::distribution-center.distribution-center');
